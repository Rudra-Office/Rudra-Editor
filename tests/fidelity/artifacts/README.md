# Artifact Directory

Generated validation outputs live here.

Typical artifact kinds:

- engine reference layout JSON
- engine reference page-map JSON
- engine reference PDF raster outputs
- DOM baseline scene JSON, screenshots, or other geometry snapshots
- canvas candidate screenshots or scene dumps
- geometry diff reports

Artifacts may be produced locally or in CI, depending on the validation stage.
