# Corpus Directory

Place approved source documents for fidelity validation here, grouped however the team prefers.

Suggested layout:

- `tier1/` core pagination documents
- `tier2/` structured-layout documents
- `tier3/` visual-object documents
- `tier4/` stress and multilingual documents

The corpus manifest is the source of truth for which files are active validation cases.
